# AI Workspace

This folder gives AI assistants a compact project entry point.

Use it when starting a new task, reviewing existing work, or preparing a commit.

Primary files:

- `context.md`: project snapshot and constraints.
- `workflow.md`: standard AI development workflow.
- `commands.md`: common command recipes.
- `templates/`: reusable task templates.

For full documentation, read `docs/`.
